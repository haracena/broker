import { Smooch, Permanent_Marker } from "next/font/google";

export const smooch = Smooch({ subsets: ["latin"], weight: "400" });
export const permanentMarker = Permanent_Marker({
  subsets: ["latin"],
  weight: "400",
});
